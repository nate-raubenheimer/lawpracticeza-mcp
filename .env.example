# LawPracticeZA API credentials — never commit real values.
# Live calls are not implemented yet (see APP-131). Tests skip live access
# unless these are set later with an explicit live flag.

LPZA_DATABASE=
LPZA_LOGIN_CODE=
LPZA_PASSWORD=

# Optional. Defaults to https://lawpracticeza.com/api
# LPZA_BASE_URL=https://lawpracticeza.com/api
